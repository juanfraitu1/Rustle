# O1 / O2 — the open queue as of 2026-09-09

The 09-05 L1–L6 queue is CLOSED (§6fh–§6fk) and so is the O3 flag pass (§6fl–§6fn). This file is what
remains, ordered. Sources: `O1_DEFINITION_SWITCH.md` §4 (G1–G5, Q9), `ROADMAP_O1_O2.md` tail, and the
2026-09-08 advisor session (§6gs–§6gu).

## A. Blocking, and the user's decision — not mine to make
| id | what | why it blocks |
|---|---|---|
| **G1** | **Two O1 definitions coexist.** `THESIS_OBJECTIVES.md` still defines a family as a γ-quasi-clique of E_r; what ships is MCL + SD core + read-supported units. A restatement is drafted (`O1_DEFINITION_SWITCH.md` §5) and has been **pending since 09-05** | every framing document, the chapter, and every answer to the advisor inherits whichever wording is current. ⚠ This is the single highest-value item and it costs no compute |
| **G2** | "at the RNA level" is true of the NODE only — five RNA-membership tests failed | ships with G1 |
| — | **O2 scope limit undocumented (NEW, §6gt)**: `copy_assign` refuses a family spanning two chromosomes. Human NPIP MCL0 (26 chr16 + 1 chr18) errored rather than truncate. Given reach ≈0.55, dispersal is common | the O1 family and the O2-assignable family are different objects and `THESIS_OBJECTIVES.md` does not say so. Register 767 |

## B. Cheap now — tonight built the instruments
| id | what | status |
|---|---|---|
| **G3** | the constants 0.70 / 0.30 / 300 bp / size 3 / 50 kb are unjustified | ⭐ **the inflation sweep (§6gu-adjacent, TBC1D3) and the Jaccard threshold sweep are exactly this instrument** — extend both to the identity and coverage floors on the anchors and G3 closes |
| — | **identity-weighted density** (advisor criticism 5) | proposed and shown to differ (0.818 vs 0.724 on TBC1D3); **not validated, not swept, not priced on a held-out family** |
| — | **genome-wide alignment runtime** | killed at 8 min for memory contention; the only cost regime where the Jaccard prefilter argument bites. Just re-run `minimap2 -x asm20 … seedgw/allgenes_gw.fa` under `/usr/bin/time` with nothing else running |

## C. The real scientific gap in O1
| id | what |
|---|---|
| **G5** | **RNA admission of unannotated loci is not implemented.** The annotation node loses ~4 % of Soto members at any-overlap that the reads found (§6ev, P3 FAILED at 0.66–0.70 detection). This is also where O3 lives |
| — | from the O3 flag pass, **pending user decisions**: read-linked admission of partial paralogues into O1 · the chain rule for members whose reads fall outside the chain · the 44 nested unit pairs · the 102 annotated-no-unit loci back into O1 |

## D. O2
| id | what |
|---|---|
| **G4** | the O2 certificate is conditional on the column set; D3 pre-registration (row 691) |
| — | **excision arm never run against competitors** (bakeoff PREREG P4). §6ff measured 100 % abstention for us on copy removal; StringTie/flair/isoseq structurally cannot abstain. ⚠ Needs reference-level excision (mask the copy, re-align, run all tools on the same substrate) — `bench/o2_excision.py` removes a copy from OUR catalog, which has no analogue in tools that have no catalog, so reusing it would quietly favour us |

**Status (evening, §6hf):** the four-step O2 plan is complete — excision passed (§6hc), aligner-disagreement
widening refuted (§6hd), indel PSV columns refuted (§6he), defaults decided by the user (drop-indels ON, the
other two OFF) and headline numbers re-based: human 1,143 contested = 230 / 531 / 382, gorilla MCL1 33 = 4 / 28 / 1.
Open from the indel test, not applied: terminal-gap exclusion inside `--indel-psv`; a pairwise `bk` (shared machinery).

## E. Benchmark loose ends from the advisor session
- **gorilla isoseq collapse is unusable** — `GGO_OR6737` is TESTIS, our substrate is fibroblast. Re-run the identical job with `MAPPED=GCA_029281585.2_flnc_mm.bam` (3 min 38 s). Until then no gorilla isoseq number exists.
- **flair arm never ran.** The driver extracted the whole BAM (4.9 GB fastq); the scoped version needs only the ~16 k molecules that get scored. P1–P3 of the bakeoff PREREG are untested and §6gs remains a source reading.
- ⚠⚠ **`fam_MCL2_073244` (64 copies) is still held back and unexamined** (trap 15). The bakeoff framing was developed on MCL1 + human MCL0; it is not validated until the held-out family is scored.

## Recommended order
1. **G1 + G2 + the O2 scope limit** — a documentation pass, no compute, unblocks everything else.
2. **G3** — extend tonight's two sweeps to the identity/coverage floors; the code exists.
3. **The held-back family** — score it before any bakeoff claim is repeated.
4. **G5 / O3 admission decisions** — the real science, and the largest.
