# Pending work — resume list (written 2026-09-17, end of session)

Supersedes nothing: `docs/PENDING_2026-09-16.md` items are still open unless marked here.

| # | item | state | next step |
|---|---|---|---|
| 1 | **Readthrough rule — PARKED by the user (2026-09-17)** | Measured and written as opt-in §0★★★.7e: the redundant-containment form drops 178/128/92/20 nodes at f = 0.50/0.80/0.90/1.00 and **no member at any f**; the blunt form deletes the NPIP member PKD1P6-NPIPP1 and is refused. It is the standing obstruction to every DNA-level certificate (PKD1P–NPIP records hold NPIP's boundary at 0.999363 once the junction conjunct removes the small records), and a containment rule cannot see PDXDC2P-NPIPB14P because NPIPB14P never became a node. | **2026-09-18: the empirical half is answered — PKD1P6-NPIPP1 is a REAL fused transcript** (110 Iso-Seq reads carry the NPIP-half -> PKD1-half junction chr16:15,120,015-15,126,650, all MAPQ 60, canonical GT-AG; 4 such fusions annotated on chr16, 2 expressed; §6m1 addendum). So the blocker cannot be removed by deleting a mis-annotation. What remains is purely the definitional question: is a record that contains a copy itself a copy, and how does a node that genuinely belongs to TWO parent families sit in the lattice? Needs a node rule (overlapping/soft membership or an explicit chimera node), not a threshold. |
| 2 | **Junction reading for gene-body chains — UNRESOLVED** | Two independent implementations disagree on 358 of 42,622 witness rows (chains whose donor and acceptor come from different member records). 262 k=2 admissions move; no h_join and no member-pair count changes either way. | Settle which reading is correct (one member record must carry both ends, vs the chain's merged column map) before t_J is used anywhere as more than a diagnostic. |
| 3 | **Held-out test of the lattice** | §0★★★.6 reserves human chr8/9/10/11 and a fresh gorilla contig set; chr8-11 is double-reserved with the deferred layer-order study. FAM90A (chr8) was scouted and rejected for the junction claim — its subfamily split IS a first-intron difference (`[[project_fam90a_parked]]`). | Pick a family whose published per-copy truth is independent of intron structure, or test a different level. Resolve the chr8-11 double reservation in BOTH documents first. |
| 4 | **Subfamily cut windows** | Recorded as report-only in §0★★★.7d: four Iso-Seq groups simultaneously exact in (0.997129, 0.997680] — three of them new under the conjunct, B15 already exact in the baseline; NPIPB missing the shipped 0.98 by 0.000559. A pre-registration draft exists (`subfam_prereg/PREREG_DRAFT_V2.md`, 526 lines) but is blocked on a usable truth. | Only with a held-out substrate and a truth sized to the family (see the metric trap in §6l9). |
| 5 | **FAM90A reserved for O2/O3** | Truth obtained from Bosch 2007 (per-copy, prose): subfamily I = FAM90A1 + HsaCopy23/24; II = HsaCopy1-22. Unusable for the junction rule; 21 CHM13 records, SEDEF identity to 0.9958, copy-number polymorphic, almost no testis expression. | Use as an O2 copy-assignment case and an O3 detect-and-flag case. HsaCopy1-24 are hg17/hg18 names — the CHM13 mapping still needs Supplementary Table S1. |
| 6 | **De novo node construction** | §6l8 closed the NPIP line with NO CANDIDATE; §6l9 adds that the de novo arm carries no junctions to test with (7 of 12 gorilla member nodes have zero). | The gap is still node construction. Any new attempt should be judged family-level, on a truth sized to the family, with a gate that passes the annotated arm first. |

Working rules unchanged: max 5 agents per task; WSL2 one heavy job foreground; declarations before numbers; `TMPDIR` on /mnt/linuxdisk for shared-definition runs.
