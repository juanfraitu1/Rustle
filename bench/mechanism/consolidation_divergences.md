# Consolidation divergences (deliverable B)

Consolidations attempted whose byte-identity gate FAILED — kept both impls, deferred to
deliverable C. Empty means every attempted consolidation was byte-identical.

| B2 item | canonical target | what diverged (families/rows) | deferred to |
|---|---|---|---|
| B2.2 EM | em_assign_family | gstm.quant.tsv abundances differ (soft_quantify_em QUANT_ERROR=0.01/100-iter vs em_assign_family error_rate=0.003/eps=1e-6/200-iter — different error model AND convergence; entangled with the 5-epsilon inconsistency). Observed on the gate corpus: CAFAM0 copy_index 1 abundance 0.0564 (soft_quantify_em) vs 0.0565 (em_assign_family), all other rows unchanged — small but gate-detectable (md5 strict). | deliverable C |
| B2.3 >=2-loci | distinct_loci (reciprocal-50%) | Mechanical gate reported PASS on the corpus (cat.families/copies.tsv byte-identical) but this is VACUOUS, not a proof of equivalence: debug instrumentation showed the gate corpus reaches `distinct_locus_reps`/`distinct_locus_reps_recip50` exactly ONCE (GWFAM0, 5 copies, all mutually non-overlapping on NC_073248.2) — zero same-chrom overlapping pairs were ever exercised, so the collapse step is a no-op under BOTH predicates by construction; the GSTM region (NC_073224.2, the known 3-copy overlapping family) never reaches this function without `--homology-primary`. Direct empirical proof the predicates DO diverge: run both on the existing in-repo fixture `distinct_locus_reps_collapses_same_locus_keeps_disjoint` (wide locus 1000-9000/8000bp + nested fragment 4000-4600/600bp, 600bp overlap, same chrom+strand, + a disjoint paralog at 50000) — any-overlap (distinct_locus_reps, live): collapses fragment into wide -> 2 loci `[1000, 50000]`; reciprocal-50% (distinct_loci logic): overlap 600 < 0.50*8000=4000 required of the wide side -> does NOT collapse -> 3 loci `[1000, 4000, 50000]`, a spurious extra locus. 1 family / up to 1 extra copy changed on this fixture (not on the live gate corpus, which the gate doesn't stress this way). Routing was reverted; gate re-confirmed PASS against the frozen baseline post-revert. Treated as a genuine divergence (not landed) despite the raw gate verdict, because the verdict was proven vacuous on this corpus. | deliverable C |
