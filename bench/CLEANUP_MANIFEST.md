# Cleanup manifest — what is data, what is derived, what is cruft

**Date:** 2026-07-09. **Nothing has been deleted.** This file marks candidates and records how to regenerate
anything reclaimed. Scratch root: `/home/juanfra/winloci_scratch` (**62 G**, 436 files + 73 dirs).

Review this list, then delete with the commands at the bottom. Anything in **REVIEW** must be resolved by a human
first — those files are referenced by scripts in this repo.

## KEEP — primary data, irreplaceable

| size | path | why |
|---|---|---|
| 11 G | `GGO_mm.bam` (+ `.bai`) | THE substrate. `-N 50 -Y --eqx`, 63% secondary. Re-alignment is hours. |
| 3.4 G | `GGO.fasta` (+ `.fai`) | T2T gorilla reference. |
| 662 M | `GGO_genomic.gff` | annotation — every cross-check in `bench/*.md` uses it. |
| 403 M | `GGO_tx.gff` | transcript annotation. |
| 926 M | `mGorGor1.mat.cur.20231122.fasta.gz` | gorilla haplotype assembly — the `asm_hapCN` copy-number **oracle**. |
| 880 M | `mGorGor1.pat.cur.20231122.fasta.gz` | ditto (paternal). |

**Total kept: ~17 G.** These are the only files whose loss would cost real time.

## DELETE — derived, regenerable, unreferenced

| size | path | why safe | regenerate |
|---|---|---|---|
| **12.7 G** | `GGO.splice.mmi` | minimap2 index of `GGO.fasta` | `minimap2 -x splice:hq -d GGO.splice.mmi GGO.fasta` |
| **9.6 G** | `mGorGor1.mat.asm20.rebuild.mmi` | index of the maternal assembly | `minimap2 -x asm20 -d <out>.mmi mGorGor1.mat.cur.20231122.fasta.gz` |
| **9.1 G** | `mGorGor1.pat.asm20.mmi` | index of the paternal assembly | `minimap2 -x asm20 -d <out>.mmi mGorGor1.pat.cur.20231122.fasta.gz` |
| **1.5 G** | `mGorGor1.mat.asm20.mmi` | superseded by `.rebuild.mmi` (same input, older) | as above |
| 1.9 G | `biser_tmp/` | BISER scratch. **0 references** in the repo. | rerun BISER |
| 1.6 G | `GGO_orig_defaultcap.bam` | superseded alignment. **0 references.** Memory: "correct BAM = GGO_mm". | realign |
| ~0 | `hh.fasta.mmi` | index of a scratch fasta | trivial |

**Reclaims ~37 G**, of which 33 G is minimap2 indices — pure caches, rebuilt in minutes.

## REVIEW — derived, but *referenced by repo scripts*

Do **not** delete without deciding whether the producing script still runs.

| size | path | referenced by |
|---|---|---|
| 1.1 G | `thin_loci_genome_wide.tsv` | `bench/annotation_seed_extend.py`, `bench/seed_extend_minimap2_param.py`, `bench/rescue_missing_oracle.py` (+3) |
| 1.9 G | `topup/` | 4 repo files |

If the producing script is recorded and cheap, these become DELETE. If not, they are effectively primary data and
belong in KEEP — decide, don't guess.

## Session output directories — safe to delete, but cheap to keep

`rt/ famcheck/ txdump/ hp_delta/ yag/ ilctl/` total **6.9 M**. Every one is a `copy_assign` output regenerable
from the commands in the corresponding `bench/*.md`. `k0flank/` (4.2 M) is regenerated by
`python bench/sim_k0_flank.py`. `uniqmap/` holds the probe region lists — 204 K, keep.

Deleting these reclaims nothing meaningful. Left alone.

## Repo working tree

52 untracked files, **5.0 M** — mostly figures (`bench/slides/*.png`, `*.pptx`) and analysis TSVs from earlier
sessions. Two decisions:

- `bench/ri_kmer_ckpt.npz` — **103 MB**, already added to `.gitignore` after it broke a push (GitHub's 100 MB
  limit). Not tracked. Delete or keep locally; it must never be committed.
- The rest are analysis by-products. They are already untracked and cost nothing. Ignore rather than delete, so
  `git status` stays readable.

## Commands (run only after reviewing the DELETE table)

```bash
cd /home/juanfra/winloci_scratch
# 33 G of minimap2 indices — caches, rebuilt in minutes
rm -f GGO.splice.mmi mGorGor1.mat.asm20.rebuild.mmi mGorGor1.pat.asm20.mmi mGorGor1.mat.asm20.mmi hh.fasta.mmi
# unreferenced scratch
rm -rf biser_tmp
rm -f GGO_orig_defaultcap.bam
```

Rebuild an index on demand:

```bash
minimap2 -x splice:hq -d GGO.splice.mmi GGO.fasta      # ~10 min, 13 G
```

## Guard against recurrence

`.gitignore` now carries `bench/*.npz`. Consider adding `bench/slides/`, `bench/*.pptx`, `bench/*.png` if those
figures are never meant to be tracked — but check with the author first: some are referenced from `bench/*.md`.
