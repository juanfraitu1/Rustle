# Adversarial review #4 — non-circularity, O1–O5 attainment, loose ends (2026-06-29)

38-agent workflow (hostile examiner → 3-skeptic refute panel → synthesis). **82 findings: 28 confirmed, 52 partial, 2 false alarms.**

## Final verdict

# FINAL DEFENSE-READINESS VERDICT

## (1) OVERALL VERDICT: DEFENSIBLE-WITH-HONEST-REFRAMING

The thesis has a real, defensible spine: a relational identifiability theory (O5 Thm4 bridge making the shipped min_p gate a load-bearing per-read certificate; MCC=χ(H); NP-hardness), a calibrated assign-or-abstain gate whose abstentions are provably the identifiability floor (99.7% δ=0 impossibility — the one place O2 survives hostile attack), a relational family definition with genuine source-orthogonal DNA corroboration (real SEDEF segdup map, verified to exist), and a controlled synthetic result that is exact (460/460, collapse 200/200). That is a passing thesis — but ONLY if every headline is rescoped to what the artifacts support. It is decisively NOT defensible *as currently headlined*: the central O2 claim, the O3 core count, the O4 "copies," the "threshold-free" O1 claim, and the O5 capstone are all overclaimed, and three of them rest on verifiably-false or missing artifacts an examiner will catch in minutes. It is also not NOT-YET — the substance is delivered; the problem is labeling and presentation, which is fixable in low-effort wording passes. Hence the middle verdict, unambiguously.

## (2) THE SINGLE BIGGEST RISK

The central empirical claim — that on real gorilla RNA a read is assigned to its TRUE copy of origin more often than chance — has ZERO fully-independent confirmation anywhere in the repo, and every real-data path loops through minimap2, the T_CORE/core_recip RNA catalog, or a simulation generated under the gate's own inference model. This is survivable by honest triangulation framing. What makes it the biggest risk rather than a managed one is the COMPLETENESS-CRITIC finding that the actual defense artifacts — the slide decks — re-assert the exact refuted claim three times ("copies & columns from DNA, never from the RNA itself / no RNA self-reference"), draw the de_max=0.05 ceiling as a line while captioning it "not a threshold," and call the tuned δ=0.005 knife-edge "derived, not tuned ... confirmed empirically." The committee watches the deck, not the honest bench/*.md files. So the risk is not merely circularity — it is that the presentation layer actively asserts the strong version of precisely the claims the candidate's own internal analysis disproves. An examiner who opens the verified TSV, the missing P1_P4_RESULTS.md, or the SOR table catches a self-contradiction, and one caught falsehood reframes every honest disclosure as damage control.

## (3) THE THREE THINGS THAT MUST HAPPEN BEFORE WALKING IN

1. **Rewrite the slide decks to match the honest internal docs.** Delete the three "copies/columns from DNA, never RNA / no RNA self-reference" assertions (the copy SET is the RNA catalog — this is the single most damaging on-slide falsehood); remove "thresholds derived not tuned / not a threshold / first error at 0.007 confirms it" and "MIN_READS=3 derived not tuned"; relabel hardcoded 97.2%/460/460 with their honest qualifiers; reconcile the contradiction where one deck claims domain-sharers are "excluded by construction" while another deck demonstrates OCLN/SEPTIN7 (3,369 reads) disproves exactly that; and ADD a limitations slide. The decks, not the markdown, are where the risk lands.

2. **Purge every verifiably-false label, repo-wide.** Relabel "accuracy" → "held-out confirmation / Nx-over-chance" for F3 (97.2%) and H3 (80%); strike silver 99.8%/99.9% from all headline tables or annotate inline "MAPQ>0 only, 17.7% of reads, circular, not accuracy"; either recompute-and-COMMIT the O3 genetic-core artifact or retract "~77" (its sole citation, bench/P1_P4_RESULTS.md, does not exist and the committed TSV is 120 transversion rows); reframe all O4 "copies" → "candidate expressed divergent MHC haplotypes pending DNA parCN" and retire PSMD2/DAXX as "airtight" (both fail the project's own SOR filter, 10.45/7.08). These are the trivially-catchable, fatal-if-found items.

3. **Pin the build-vs-run truth and the O2 headline number.** State explicitly which engine the shipped `rustle` pipeline runs — psv_linkage (tau-gate, 46.9%) is wired into main.rs/pipeline.rs, while the 63.9% significance-gate headline is the default only in the standalone binary; do not let "the O2 result" be a non-default engine's number. Reconcile 46.9 / 63.9 / 69.3 / 75.1% on ONE real substrate; scope "threshold-free" to "no ABSOLUTE-similarity cutoff (relational tie), with a tuned tie-window — min_reads=3 and de_max=0.05 persist even in the sig path"; and commit reproducer output tables for the four corroboration numbers (the crosstab's hardcoded final.bed path is broken and crashes from a clean checkout).

## O1–O5 scorecard

### O1 — RNA family definition (read-conflict graph) — **ATTAINED_WITH_CAVEATS**
- **Caveat:** 'No similarity threshold / removes the last threshold' is the load-bearing OVERCLAIM and must be retired, exactly as the prior defense audit said. (1) BUILD-vs-RUN: the threshold-free significance-edge (sig) path is default-OFF (needs RUSTLE_CONFLICT_SIG=1); every headline catalog was produced by the THRESHOLDED de_tied predicate with hand-set delta=0.005 / de_max=0.05 / min_reads=3 — and the bakeoff itself admits delta=0.005 is a 'knife-edge' (0.01 re-admits CNN2, 0.02 re-admits EEF1A1). (2) The clean 'connected component = family' object is overridden in production by 4-5 thresholded downstream filters (Louvain community split, same-strand, span<=win, min_copies, exon-sum homology asm20 id>=0.80/cov>=0.50) that exist precisely because the raw cross-chrom graph OVER-MERGES via Alu/repeat bridges — so 'never groups domain-sharers by construction' is false at scale. (3) External validation is largely similarity-validates-similarity (protein mmseqs is the user's own clustering; annotation 70.9% is partly circular; SEDEF 73%/90% has NO negative-control / background-rate baseline, and final.bed covers a large fraction of the duplicated genome so 90%-in-segdup may approach the chance hit-rate for ANY co-located pair). Only genuinely-independent witness is Ensembl Compara: 12 human pairs, 33% confirm, precision-only — far too thin.
- **In a defense:** YES, but scoped. Claim: 'a relational, identifiability-driven family definition that flags the loci where copy-assignment is contested, producing a genome-wide catalog with real DNA (SEDEF segdup) corroboration.' Do NOT claim 'threshold-free' as a headline, and do NOT attribute the shipped catalog to the sig path. Disclose the tuned constants, the downstream homology refinement, and that SEDEF/protein/annotation lack a negative control (so 73%/85%/90% are 'fraction confirmed', not calibrated precision). A real SEDEF/Compara run WITH controls on a null set is the outstanding work.

### O2 — per-read copy assignment (significance gate) — **ATTAINED_WITH_CAVEATS**
- **Caveat:** There is NO non-circular real-data ACCURACY for O2 — the central thesis claim (a real gorilla RNA read is assigned to its TRUE copy of origin more often than chance) is demonstrated only on synthetic data and self-consistency. (1) Every real-data 'accuracy' is the silver standard, which is doubly circular: its ground truth IS minimap2's own primary placement, and it is computed only on MAPQ>0 reads = 17.7% of reads (36,551/206,186) — structurally BLIND to the 82% MAPQ-0 mass the gate exists to resolve. (2) The only non-circular checks are synthetic (sim5x ladder; held-out crossval), and the sim's generative model (i.i.d. substitutions, independent PSV columns, err=0.003) EQUALS the gate's inference model, so 100% tests implementation consistency, not real-RNA robustness (correlated errors, editing, het). (3) The 'first non-circular real-data accuracy 97.2%' (DNA decode) is self-CONSISTENCY not accuracy (train/test halves of ONE read's columns agree on a signature; no true-origin label; 2.2x over chance, K=2-coin-dominated; copies still from the RNA catalog; minimap2 still selects reads). (4) The resolution-improvement bound is ~0% on real GGO: the ambiguous mass is K=0 identical-copy (delta=0) — 'where resolution is possible it is unnecessary; where it is needed it is impossible' — so the gate abstains honestly but does not demonstrably RESOLVE hard real-data ambiguity. Secondary: the headline assigned-rate for the 'same' catalog is reported as 46.9% / 63.9% / 75.1% with the favorable 63.9% led; '99.3% of decisive' uses a denominator 8,017 reads tighter than its prose ('any distinguishing evidence' is 93.6%); the alpha bound is a union bound that is anti-conservative on real RNA (editing, correlated mismatches) and verified only in MC sim to a factor of 3.
- **In a defense:** PARTIALLY. Claim: 'a calibrated, threshold-honest assign-or-abstain gate with a per-read significance certificate, whose abstentions are provably the identifiability floor (99.7% delta=0 impossibility) and which resolves collapsed MAPQ-0 segdups in controlled simulation (200/200).' Do NOT claim real-data copy-assignment ACCURACY; do NOT put silver 99.8% in a headline table without the 'circular, easy-regime-only, 17.7% denominator' label; do NOT call the DNA decode 97.2% 'accuracy' (re-label 'held-out confirmation / 2.2x enrichment'). The honest summary is: mechanism + calibration attained and real-data-vindicated as a floor; real-data resolving-power accuracy is synthetic/self-consistency only.

### O3 — allele-specific junctions (ASJ) — **MECHANISM_ONLY**
- **Caveat:** Every defensible headline is undermined, and the shipped engine has NO artifact guards. (1) The headline 'genetic core ~77' is NOT reproducible: its only documented evidence (bench/P1_P4_RESULTS.md, the 17/18 LOC* masquerade run) is a DEAD CITATION — verified missing this session; the committed verified TSV's high_confidence flag is exactly the 120 transversion rows (verified), encoding NEITHER the 120->77 copy-confound exclusion NOR strand bias NOR chimera. (2) 'non-LOC* => genetic' is an unsupported inference: transversion rules out editing, NOT the copy/paralog confound, and 22/76 (29%) of the named core FAIL the project's own SOR strand-bias filter. (3) The flagship 'mechanistically airtight' calls PSMD2/DAXX both FAIL SOR (10.45, 7.08, ~5x over longcallR's cutoff; the user's own memory F5 says 'retire as headlines'), the dinucleotide-mechanism claim was already RETRACTED (0/475 anchors on a core splice dinucleotide), yet 'dinucleotide / airtight' survives in the headline. (4) ALL controls (SOR, copy-separator, chimera, microhomology) are post-hoc Python over a frozen TSV — grep finds ZERO strand/sor/chimera/microhomology in the Rust engine — so the production tool emits unguarded calls. (5) The multi-SNP 453 re-imports the copy confound (46% LOC*) and its only control, phase_qual, is MAXIMIZED by the confound (a clean 2-copy paralog bipartitions exactly like a het). (6) The flagship results table is led by 6 LOC101141065 distal full-switch rows that are 'chimera-not-excluded' with no committed flag.
- **In a defense:** ONLY as a method, not as a validated result set. Claim: 'a phasing-free allele->junction linkage measurement implemented end-to-end, with an editing control.' Do NOT present any specific call (PSMD2/DAXX, LOC101141065, the ~77 core, the 453) as validated allele-specific junctions: the core count is unbacked by any committed artifact, the flagships fail the project's own strand-bias filter, the copy-vs-allele confound is uncontrolled for the named core, and the shipped engine ships calls with no guards. A hostile examiner who opens the verified TSV finds 120 transversion labels, not 77 genetic calls.

### O4 — reference-absent / collapsed copies — **MECHANISM_ONLY**
- **Caveat:** The shipped mechanism is DETECT+FLAG only — ZERO placement, ZERO validated copies on real data — and every promoted 'copy' is an offline candidate pending a DNA check that was never run. (1) The single experiment that would convert candidate->copy (DNA copy-number / parCN) produced NO output: verified GGO_sedef_out/ has seeds+align job dirs but NO final.bed; 0 DNA-confirmed copies; the resolver is absent, not merely deferred. (2) The 'divergence => copy' promotion rule is CIRCULAR: reads are SELECTED for >=12 reference-divergent ALT columns, their consensus is then 'discovered' to be divergent, and the het cutoff '<0.5% => het' is FALSE at the MHC where all 4 'copies' live (hyperdivergent diploid alleles routinely differ 4-20%). 'Protein confirms COPY' is unsupported — protein confirms only endogenicity; honest framing is '4 expressed divergent MHC haplotypes', and DSFAM292 (97 aa, depth 1.17x, 8 reads, 20.4% div) sits on the rejected-artifact boundary, so '4' is really '3 weak + 1 borderline'. (3) The project's OWN adversarial analysis refutes the detector: a real second genomic copy must multimap (frac_mq0>0), and the joint confound gate (frac_mq0>=0.3 AND n_coseg>=8) collapses the signal to 0 loci ('GO/NO-GO: NO-GO'); frac_mq0 is verified ABSENT from the promotion scripts, so 18/4/905 are produced by the precise detector already proven to be het-dominated. (4) The 7.39% FP 'bound' equals the 7.93% genome-wide background (enrichment ~1.0x = no signal) and is non-reproducible (missing gff). (5) The 905-collapsed count is the weakest gate, internally contradicted by the doc's own 'do not assert the flagged pool as a copy set', and the live artifact shows 885 (and is now absent from scratch). (6) The only end-to-end recovery exhibit is SYNTHETIC and in the 1-primary regime where --recover-copies is byte-identical (contributes nothing); the true 0-primary reference-absent regime is asserted, never exhibited.
- **In a defense:** ONLY as a screen producing candidates. Claim: 'a detector that FLAGS candidate collapsed/reference-absent loci (4 endogenous expressed divergent MHC sequences + dispersed candidates) pending DNA copy-number validation.' Do NOT use the word 'copies' for any of them, do NOT cite 905/885 as a result, do NOT present the MHC four as confirmed paralogs. Every copy-vs-allele claim rests on a DNA parCN check that has produced no output. This is the weakest objective: mechanism + offline candidate generation, no validated copy.

### O5 — combinatorial theory (MCC=chi(H), bridge, MWCA / facility-location) — **ATTAINED_WITH_CAVEATS**
- **Caveat:** The advisor-aligned CAPSTONE claims that close the unification narrative are asserted, not proved or machine-checked, and most of the theory is decorative w.r.t. the product. (1) Thm 7 'min_p = LP dual / complementary-slackness witness' has ZERO formal or computational backing: the check hardcodes delta=1, sets minp=0.001, and asserts a tautology (len(N[r])==1 and 0.001<1.0) — no LP dual is ever read. (2) Thm 6 'LP-rounding capstone' performs NO rounding — the (1-1/e) bound is plain textbook greedy (Nemhauser-Wolsey-Fisher); the only novel piece, the conflict-constrained Canzar constant (~0.19), is one unproven sentence. (3) The MWCA weight in the check is w(r,c)=#observed-columns (copy-INDEPENDENT), so the 'principled non-1/k evidence weight' selling point is never exercised — submodularity is verified on degenerate set-coverage. (4) Thm 7's keystone 'Strong Separation <=> |N(r)|=1' is FALSE in the (<=) direction (reproduced counterexample), so the '166 Strong-Separation instances' is actually the weaker unique-compatibility universe, mislabeled; integrality is checked only with a non-binding budget K=|C|. (5) Thm 4 soundness holds only in the error-free core with origin(r)∈C — both fail on the real error-laden substrate the gate runs on, so the certified soundness is for an idealization. (6) RECOVER / self-certifier have 0 call sites; Thm 2/3/5/6/7 do not run in production. (7) All machine-checks are bounded K<=3, L<=3, m<=3 — 'exhaustive' only within a tiny universe; Strong Separation is never shown to hold on real RNA non-circularly. Lemma 1 / Thm 1 are correct but elementary (clique-cover duality + graph-coloring embedding).
- **In a defense:** YES, as the thesis's strongest contribution, but scoped. Claim: 'a clean graph-theoretic identifiability theory (family=coloring, NP-hard, Strong Separation as a sufficient unique-recovery condition) with a per-read min_p certificate that is bridged to and load-bearing in the shipped gate (Thm 4).' Do NOT claim the capstone as proved: min_p = LP dual witness, LP-rounding -> Canzar constant, and the non-1/k evidence-weighted MWCA are CONJECTURES/DIRECTIONS, not results — their machine-checks are vacuous, degenerate, or check a weaker condition than labeled. Do NOT say 'exhaustively verified for all K' (bounded K<=3; all-K rests on prose proofs). Present Thm 4 + the identifiability framing as the delivered core; present F4/Thm 5-7 as the program, not the proof.

### Cross-cutting — non-circularity of real-data corroboration (the #1 risk) — **ATTAINED_WITH_CAVEATS**
- **Caveat:** NOT ONE source is a fully-independent, real-gorilla, true-read-origin accuracy — the central empirical claim rests on synthetic data + self-consistency. Tracing every truth source: silver = minimap2's own primary, self-labeled CIRCULAR, only on the easy 17.7% MAPQ>0 reads; sim = non-circular labels but the generative model equals the gate's inference model; DNA decode = self-CONSISTENCY (no true-origin label) on RNA-catalog-defined copies with minimap2 still selecting reads; held-out CV = self-consistency; SEDEF segdup (final.bed) is real but has NO negative-control/background baseline and shares the very shared-exon block that drives RNA over-merge (so it cannot catch over-merge); protein mmseqs = the user's own clustering; Compara = 12 human pairs, 33%, precision-only; annotation 70.9% = partly circular (79% LOC* provisional). The crosstab as committed does not even reproduce (hardcoded final.bed path is broken; result artifacts are not committed). Every real-data path routes through either minimap2 or the T_CORE=0.13 RNA catalog -> closed loop; the only escape (sim) is generated under the inference model.
- **In a defense:** Claim only the honest triangulation: 'no single external oracle exists; several independent partial witnesses (planted-label sim, DNA-derived per-copy signatures generalizing above chance, real SEDEF segdup overlap) concur.' Do NOT present ANY single real-data number as independent true-origin accuracy. The decisive exits — a gorilla-native Compara/OrthoFinder positive set, DNA parCN, and a cluster-scale SEDEF/BISER run WITH a negative-control null — remain outstanding; until then the #1 circularity risk is mitigated by convergence, not closed.

## Circularity verdict — PARTIALLY_CIRCULAR, dominated by a self-consistent core exactly as the memory warns

| truth source | verdict | why |
|---|---|---|
| Silver standard (minimap2-primary agreement, 99.8%/99.9 | CIRCULAR | Ground truth IS minimap2's own primary placement (denovo_pipeline.rs:679 uniq_agree += best_copy==r.mapped_copy), scored only on MAPQ>0 uniq |
| Sim-planted ground truth (sim_genome / sim5x, O2 460/46 | PARTIALLY_CIRCULAR | Labels are genuinely non-circular (planted true copy of origin, nothing borrowed from pipeline output) -- this is the only place true-origin |
| DNA-catalog supervised decode (F3, 97.2% held-out / 2.2 | PARTIALLY_CIRCULAR | Two independent elements, two circular elements. Independent: the per-copy allele SIGNATURES are DNA-derived (dna_psv_catalog.py aligns each |
| Held-out-PSV cross-validation (H3, 80% / 1.6x-6.4x chan | PARTIALLY_CIRCULAR | Same self-consistency construction as F3 (copy_assign.py crossval): splits a read's distinguishing columns, assigns on one half, confirms on |
| Held-out-DNA-column cross-validation | PARTIALLY_CIRCULAR | This is the column-split inside F3, not a separate oracle. DNA supplies the column allele values (independent), but the train/test halves co |
| SEDEF segmental-duplication map (final.bed, genome self | NON_CIRCULAR | Construction is genuinely orthogonal: a genome self-alignment computed from the DNA assembly alone, never seeing RNA reads, minimap2-RNA ali |
| Protein homology clustering (mmseqs, 199,828 paralog pa | PARTIALLY_CIRCULAR | It is the user's OWN clustering of the same representative proteins at hand-set bars (fident>=0.30, recip-cov>=0.50), self-conceded 'mmseqs  |
| Ensembl Compara paralogy (12 mappable pairs, 33% confir | NON_CIRCULAR | Genuinely independent and the cleanest external source: human-curated gene-tree + species-tree-reconciliation paralogy that never sees the p |
| Annotation overlap floor (RefSeq/Gnomon, 70.9% / DNA-sp | PARTIALLY_CIRCULAR | Self-admitted partly circular: the 89.2% DNA-span figure has the span as a SUPERSET of the exons that defined the family, and the RefSeq/Gno |
| DNA-pairwise shared-block test (crosstab asm20 200bp/90 | PARTIALLY_CIRCULAR | Starts from the RNA family's member list and asks whether members share a >=200bp/>=90% block -- but a shared exon/domain/repeat block IS th |
| Held-out DNA columns as alignment-error judge (new work | PARTIALLY_CIRCULAR | Presupposes the DNA catalog is the COMPLETE truth to label a base 'off-signature artifact' and to adjudicate -- but the project's own O4 pre |

**One fully-independent confirmation?** The answer splits by claim. For the WEAKER prerequisite 'the families are real duplications' (O1 grouping): YES -- the SEDEF genome-self-alignment segdup map (verified to exist on the gorilla T2T assembly, 253,030 pairs) and the Ensembl Compara human-curated paralogy spot-check are genuinely source-independent (they never see the RNA reads), though both are grouping-scoped, SEDEF lacks a calibrated null control, and Compara is only 12 pairs. For the CENTRAL thesis claim 'RNA reads carry copy identity' -- i.e. that on real gorilla data a read is assigned to its TRUE copy of origin more often than chance -- NO: there is no fully-independent real-data confirmation anywhere in the repo. The single experiment designed to be it (F3 DNA-supervised decode, 97.2%) is only PARTIALLY independent: its per-copy signatures are DNA-derived (good), but the copy membership is the RNA threshold catalog, read selection is minimap2, and the metric is held-out self-consistency of one read's own column-halves with no true-origin label -- so it measures internal signature consistency / above-1/K generalization, not correctness against origin. The only true-origin accuracy (sim 460/460=100%) is synthetic and generated under the gate's own model. Therefore: exactly one fully-independent confirmation exists for family REALITY, and ZERO fully-independent confirmations exist for the read->copy IDENTITY claim that is the thesis's core. The honest defense framing is 'no single external true-origin oracle for read->copy assignment; multiple partially-independent witnesses (DNA-defined signatures, above-chance held-out generalization, planted-label sim, and orthogonal SEDEF/Compara family-reality) converge,' which is weak triangulation rather than independent proof. The decisive exit -- completing the gorilla SEDEF/parCN run and a true-origin or gorilla-native Compara/OrthoFinder check -- remains unrun.

## Must-do before defense

1. **Relabel every 'accuracy' claim for the F3 DNA-supervised decode (97.2%) and the H3 held-out-PSV CV (80%) to 'held-out confirmation / self-consistency / enrichment over 1/K chance'. Remove 'first non-circular real-data O2 ACCURACY' from VALIDATION_AND_STATUS.md and make_slides.py.**  
   _Why:_ Both CVs split a single read's OWN columns train/test and check the two halves agree on a copy; there is NO origin-truth label on real GGO. A read from a catalog-absent copy force-matches and self-confirms. 'Accuracy' is a verifiably false label an examiner will catch; the correct word ('confirmation', 2.2x over chance) preserves the (real) finding. This is the single most quotable over-claim and sits on the advisor slide deck. _(Low — wording fix across ~3 docs + slides; the underlying result stands.)_
2. **Strike the silver-standard 99.8%/99.9% from every headline/corroboration table, or annotate it inline as 'easy-regime consistency w/ minimap2 primary, NOT accuracy'.**  
   _Why:_ Silver is computed on only the 17.7% of reads with MAPQ>0 (36,551/206,186), scores agreement with minimap2's own primary placement, and is structurally incapable of scoring a single MAPQ-0 read — exactly the mass the gate exists to resolve. The code itself prints 'silver = CIRCULAR'. Leaving a bare 99.8% cell next to the principled 63.9% invites an examiner to read it as validation where validation is impossible. _(Low — table edits; the caveat already exists in prose, just not adjacent to the number.)_
3. **Recompute and COMMIT the O3 genetic-core artifact, or retract the ~77 number. bench/P1_P4_RESULTS.md (the sole cited backing for the 120->77 LOC* masquerade reduction) does not exist; the committed asj_calls_verified.tsv high_confidence==1 is exactly 120 rows (the transversion set), not 77.**  
   _Why:_ Verified missing file + the committed TSV encodes none of the copy-confound exclusion. The headlined 'genetic core ~77' currently rests on a dead citation and is non-reproducible from any artifact in the repo — a trivially embarrassing catch. Either regenerate the masquerade table and commit it, or report only what asj_calls_verified.tsv supports (120 transversion calls). _(Medium — re-run scan_gene_copy_specific_junctions on the LOC* windows and commit the output TSV, or drop the ~77 prose.)_
4. **Reframe O4 '4 endogenous reference-absent MHC paralog COPIES' to '4 expressed endogenous divergent MHC haplotypes/candidates (copy-vs-allele pending DNA)'. Apply the same to '15 dispersed copies' and the '905 collapsed copies'.**  
   _Why:_ Protein BLAST confirms endogeneity, not copy status. All 4 sit in the most hyperdivergent-allele region of the genome, where a 4-20%-divergent second haplotype is the EXPECTED diploid het. The divergence=>copy rule is circular (reads were selected for >=12 reference-divergent columns, so the consensus is divergent by construction) and its '<0.5%=>het' premise is false at the MHC. DNA parCN never ran (no final.bed in the SEDEF output dir). The word 'copy' asserts the one thing the method cannot show, at the locus where it is most likely wrong. _(Low-Medium — relabel headlines; optionally run SEDEF/parCN (the real fix) which is staged but unrun.)_
5. **Scope the O1 'no arbitrary similarity threshold / threshold-free' claim honestly, and stop attributing it to the shipped 82-family catalog.**  
   _Why:_ The headline gw_conflict_catalog was built by the THRESHOLDED de_tied (delta=0.005, de_max=0.05, min_reads=3); the 'threshold-free' sig path is default OFF (RUSTLE_CONFLICT_SIG=1) and produces no headline. delta=0.005 is a tuned knife-edge (the bakeoff admits D->0.01 re-admits CNN2, D->0.02 re-admits EEF1A1 catastrophically). 'Threshold-free' is the Canzar aesthetic north-star, so this is the highest-value attack surface. Honest statement: 'no ABSOLUTE-similarity cutoff (relational alignment-score tie), with a tuned tie-window'; min_reads=3/de_max=0.05 persist even in the sig path. _(Medium — either make sig default-on and re-run the headline catalog (best), or restate the claim's scope precisely.)_
6. **State the central honest framing explicitly and lead the defense with it: there is NO single external real-data true-origin accuracy for O2 anywhere in the repo; support is (a) synthetic sim with planted labels, (b) held-out self-consistency above 1/K chance, (c) partly-circular DNA/annotation. Never present any single real number as independent accuracy.**  
   _Why:_ This is the #1 documented risk (memory: 'silver/DNA/70.9%/sim5x self-consistent; SEDEF/BISER never run'). Every real-data route loops through minimap2 (silver, read selection, F3 ref0 realignment) or the threshold catalog (T_CORE=0.13, denovo_families.tsv). The only clean exit (real SEDEF/BISER or gorilla Compara/OrthoFinder) was never run. Owning 'multiple independent partial witnesses concur; no single oracle' pre-empts the killer question; getting caught presenting a circular number as the oracle is fatal. _(Low to frame; High to actually close (requires a cluster SEDEF/BISER or gorilla Compara run).)_

## Killer questions

- **[EXPOSED]** On real gorilla RNA, show me ONE number where a read is assigned to its TRUE copy of origin more often than chance, with the ground truth coming from OUTSIDE your own pipeline — not minimap2's primary placement, not your own threshold-built catalog, not a simulation you generated under your own model. If you cannot, on what basis is O2 'attained'?
  - _Answer:_ There is none on real data, and I concede it — this is the project's #1 risk and it is confirmed in code. Enumerating every truth source: silver = minimap2 primary on the 17.7% MAPQ>0 easy reads (self-labeled CIRCULAR in copy_assign.py); F3 DNA-supervised decode 97.2% is held-out-COLUMN self-consistency (a read's even/odd column halves agreeing on a signature is necessary, not sufficient, for corr
- **[EXPOSED]** You call the 82-family read-conflict catalog 'no similarity threshold — the boundary is the alignment-score tie, a property of the data,' and claim the conflict-sig unification 'removes the last threshold.' But ConflictParams::default is delta=0.005, de_max=0.05, min_reads=3, and the significance path is gated behind RUSTLE_CONFLICT_SIG=1, OFF by default — I verified read_conflict.rs:60 ships sig:None. So the principled headline catalog was built WITH the hand-tuned divergence window you claim to have removed, and your own bakeoff calls delta=0.005 'a knife-edge against CNN2.' Is this not exactly the overclaim your prior audit said you retired?
  - _Answer:_ Conceded as a build-vs-run gap plus a half-overclaim. (1) The shipped headline catalog ran de_tied with delta=0.005 — a tuned constant (min_reads was raised 2->3 specifically to clear CNN2's NM noise floor; delta was placed below the smallest genuine-conflict gap). (2) The threshold-free sig mode is real and machine-checked default-OFF byte-identical, but it produced no headline, and it replaces O
- **[HOLDS_WITH_HONEST_CAVEAT]** Which number IS your O2 result? The same conflict-graph catalog appears at 46.9% (tau=6.9 vote engine), 63.9% (significance gate — your headline), and a co-located subset at 75.1% that does not even reproduce (the cited artifact now yields 69.3% assigned / 1.1% ambiguous, contradicting your '0.0%'). You lead with the second-highest and assert the two engines are 'identical at every K' — but that was shown only on the synthetic sim5x ladder, and on real data they diverge 17 points. Reconcile, or admit you selected the favorable engine.
  - _Answer:_ The 63.9% (131,659/206,186) reproduces EXACTLY from p1_conflict_o2.assignments.tsv, so the lead figure is real and auditable. But I concede: the doc never runs both engines on the identical real substrate and never reconciles 46.9 vs 63.9; the 'gate-equivalent at every K' claim is validated only on planted synthetic sim5x; and the 75.1% subset cite is stale (current artifact 69.3%/29.6%/1.1%, so '
- **[HOLDS_WITH_HONEST_CAVEAT]** Your 99.8% silver-standard agreement sits in the genome-wide headline table. I computed its denominator: 36,551 of 206,186 reads = 17.7% — exactly the MAPQ>0 unique-mappers that DON'T need your gate — and 'agreement' means agreeing with minimap2's own primary placement, the very aligner you claim to improve on. It is structurally incapable of scoring a single MAPQ-0 multimapper, which is the entire mass the gate exists for. Why is a metric your own code labels CIRCULAR a headline corroboration?
  - _Answer:_ It should not be headlined, and I concede the structural blindness. Silver validates the gate precisely where validation is unnecessary (reads minimap2 already placed at MAPQ>0) and is blind where it matters (MAPQ-0). The doc does self-flag it (copy_assign.py prints 'silver = CIRCULAR self-consistency w/ minimap2 primary, NOT accuracy') and redirects to sim5x — but the 17.7% denominator and the st
- **[EXPOSED]** Your O3 'defensible genetic core ~77' rests on the 120->77 reduction via a LOC* copy-masquerade analysis whose ONLY documented evidence is bench/P1_P4_RESULTS.md — which does not exist on disk (I checked; four docs cite a dead file). Your committed 'verified' TSV encodes nothing but 'transversion' (high_confidence==1 -> exactly 120 rows, not 77). And your two flagship 'mechanistically airtight' calls, PSMD2 and DAXX, both FAIL your own longcallR-style SOR strand-bias filter — I confirmed sor_pass=0, SOR 10.45 and 7.08 against a cutoff of ~2, every read on one strand — and your own memory note F5 says 'retire as headlines.' Defend the core and the flagship.
  - _Answer:_ Both are conceded. The ~77 is backed by a missing artifact and is not reproducible from anything committed; the verified TSV's high_confidence column is a pure rename of 'transversion,' encoding neither the 120->77 copy-confound exclusion nor strand bias; and the 76 named-gene core was NEVER run through scan_gene_copy_specific_junctions — the transversion control only excludes A->I editing, not pa
- **[EXPOSED]** You headline '4 endogenous reference-absent divergent MHC paralog COPIES.' Protein BLAST confirms they are gorilla MHC protein — it says nothing about copy versus allele. Your discriminator is 'genome divergence >3% => copy (too divergent for a het),' but you SELECTED the reads carrying >=12 reference-divergent ALT columns, assembled them, then 'discovered' the consensus is divergent — the divergence is a tautological consequence of the selection. These sit in the single most hyperpolymorphic region of the genome, where a 4-20% divergent second HAPLOTYPE is the expected diploid het, and your own confound-controlled hidden-collapse headroom returns 0 loci. So what is a 'copy' here that isn't a het — and why was the DNA parCN resolver never run?
  - _Answer:_ The word 'copy' is unsupported and should be 'candidate/haplotype' until DNA. Conceded: (a) copy-vs-allele is explicitly deferred to DNA parCN, and that resolver never produced output — I verified no per-O4 parCN artifact exists and GGO_sedef_out/final.bed is absent; (b) the >3%=>copy rule is invalid precisely at the MHC, where hyperdivergent alleles routinely span 4-20%; (c) the divergence is par
- **[EXPOSED]** Your capstone is the Canzar facility-location unification: NP-hard -> (1-1/e) LP-ROUNDING -> constant factor, with min_p as the LP dual / complementary-slackness witness and w(r,c) the principled non-1/k evidence weight. But your integrality 'certificate' hardcodes delta=1, sets min_p=0.001, and asserts 0.001<1.0 — a tautology; no dual vector is ever read. Your '(1-1/e) LP-rounding' is plain textbook greedy — the LP is never rounded — and the conflict-constrained Canzar constant is asserted in one sentence, unproven. And your MWCA weight is w = number-of-observed-columns, IDENTICAL for every compatible copy, so submodularity is verified on a degenerate SET-COVERAGE function that doesn't distinguish copies at all. Where is the evidence-weighted, conflict-constrained result you advertise — the part that isn't textbook?
  - _Answer:_ The textbook core is sound and verified — monotone submodularity and the (1-1/e) greedy bound are weight-agnostic structural facts that provably carry to the real LLR weights, and MCC=chi(H) / NP-hardness hold (though they are clique-cover duality plus a graph-coloring embedding — correct but elementary). But every load-bearing CANZAR-aligned claim is conceded as not-yet-established: min_p = compl
- **[HOLDS_WITH_HONEST_CAVEAT]** Your prior audit named the core risk the 'build-vs-run gap.' Your four corroboration headlines — 85% any-level, 73% SEDEF, 30.5% Soto, 97.2% decode — have no committed result artifact; the numbers live only in prose. I checked the crosstab: its default SEDEF path is hardcoded to Rustle/final.bed, which does not exist (the real 107 MB map is one directory up at Desktop/final.bed), so the committed script with no argv crashes, and every input lives in winloci_scratch outside the repo. From a clean checkout, reproduce one headline. Hasn't the gap just been MOVED, not closed?
  - _Answer:_ Largely conceded for reproducibility, with one genuine credit. The four numbers are not pinned by any committed crosstab/decode/soto output; the default SEDEF path is broken (verified: Rustle/final.bed absent; real SEDEF map at Desktop/final.bed); and inputs sit in scratch — so a reviewer cannot re-derive them from what is committed (the author can, with the right argv). Credit: a REAL SEDEF-on-GG

## What the review itself may have missed

- The three slide decks under bench/ — the actual defense artifacts the committee sees — were NEVER audited. The entire 82-finding review cited bench/make_slides.py exactly once (only to source the 97.2% number's existence). Yet make_slides.py / multicopy_families.pptx asserts ON-SLIDE the precise falsehoods the review's own CRITICAL findings refuted: fig_dna title (line 114) + slide bullet (line 215) + caption (line 226) state THREE times 'copies & columns from DNA, never from the RNA itself / no RNA self-reference' — exactly the claim the review's critical noncircularity findings proved false (the copy SET is denovo_families.tsv, RNA-derived). The de-tie figure DRAWS the de_max=0.05 ceiling as a dotted line (lines 84-85, 'de_max ceiling') while captioning it 'not a threshold' / 'not a tuned cutoff' (lines 192, 201). The alignment-error n=6-underpowered result is stated as 'MEASURED vs DNA truth ... change 0.03% of calls' fact (line 237). 460/460, 43.2%, 97.2%, 85/73/30/15, ~1%/0.03% are HARDCODED matplotlib literals (lines 97-99,109-110,122), decoupled from any artifact — so they cannot track the staleness drift the review itself found (75.1%->69.3%). The only hedge in all 12 slides is the word 'largely' (line 245); there is NO limitations/caveats slide. make_family_slides.py / family_definition_slides.pptx goes further: 'The thresholds are DERIVED, not tuned ... a measurement constant ... first error at 0.007 CONFIRMS it empirically' (lines 231-235) and 'MIN_READS=3 — derived, not tuned' (line 174) — directly contradicting the review's critical evidence that delta=0.005 is a knife-edge tuned against CNN2 and min_reads was raised 2->3 to suppress CNN2 noise.
- The three decks contradict each other and the project's own more-careful deck, and this cross-artifact inconsistency was never surfaced. make_slides.py slide 4 caption (line 197) claims 'domain-sharers (one shared exon, no tie) are correctly left out' — i.e. the by-construction immunity the review's O1 partial-finding already refuted — while make_definition_slides.py line 203 presents the project's OWN measured counterexample 'OCLN and SEPTIN7 — 3,369 reads tie them, so ~R draws an edge — yet they are not copies' and concedes '~R over-couples ... we need a second, independent test (~B backbone).' So the simplest deck headlines the clean by-construction story that the project's own detailed deck explicitly demonstrates is false.
- A whole class of recently-shipped PRODUCTION features was outside the review's window and went completely unexamined: the Bayesian per-read posterior (--posterior, copy_assign.rs:360-383, softmax(logl) over candidate copies + the 'consistent ZONE' for Tied reads), plus the intron-PSV lever (RUSTLE_INTRON_PSV, commit 0f0ecae) and intron-retention rescue (commit 2704877). The review's new_work unit scoped itself to 'the 8 recent commits' (slides through starved-copy) and these three commits fall just before that window, so no finding touches them.
- The two-engine build-vs-run question was flagged but left half-resolved. The review noted the 46.9% (psv_linkage tau=6.9) vs 63.9% (copy_assign significance) swing on 'the same catalog' but never established WHICH engine is production. I confirmed psv_linkage.rs is wired into the MAIN pipeline (main.rs, pipeline.rs, layer2.rs) while the copy_assign significance gate (the 63.9% headline) is the default only in the standalone src/bin/copy_assign.rs and the denovo conflict path (denovo_pipeline.rs:1550 AssignParams::default()). So the headline 63.9% O2 result may come from a bench/standalone binary, not the engine the main `rustle` pipeline ships — a genuine build-vs-run direction the review did not pin down.
- igv_tracks.py (commit 6f10509, the companion display tool) was not examined at all. It is low scientific stakes (pure visualization), but the deck's summary slide (make_slides.py line 246) instructs the committee to 'View it in IGV: copy_assign --gtf + reads coloured by assigned copy' — i.e. igv_tracks is part of the demonstrated artifact. Unlike the --gtf path (which the review showed cannot tag rescued RC_ copies due to a cross-pass tid join), igv_tracks keys on the assignments TSV by read_name (igv_tracks.py:37-47, 97-100), so it sidesteps the RC_ bug — meaning the IGV demo CAN show what the --gtf finding says the GTF cannot. The review never checked this consistency.
